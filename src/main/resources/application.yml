server:
  port: 80
spring:
  datasource:
    username: postgres
    password: ismoil_0709
    url: jdbc:postgresql://postgres_demo:5432/demo_bot_db
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update